storage:
  type: sqlite
  path: /data/data.db

endpoints:
  - name: app-with-ingress
    url: "https://poc-k8s.dados.rio/app"
    conditions:
      - "[STATUS] == 200"

remote:
  instances:
    - endpoint-prefix: "poc-k8s-"
      url: "https://poc-k8s.dados.rio/gatus/api/v1/endpoints/statuses"
